
{# Perfil #}

{% extends 'base.html' %}
{% load static %}

{% block title %}Meu Perfil - Sistema de Chat{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{% static 'css/usuario/profile.css' %}">
{% endblock %}

{% block content %}
<div class="row">
    <!-- Sidebar do Perfil -->
    <div class="col-md-3">
        <div class="card profile-card">
            <div class="card-body text-center">
                <div class="profile-avatar mb-3">
                    <i class="bi bi-person-circle" style="font-size: 4rem; color: #6c757d;"></i>
                </div>
                <h5 class="card-title">{{ user.get_full_name|default:user.username }}</h5>
                <p class="text-muted">@{{ user.username }}</p>
                <p class="text-muted small">{{ user.email }}</p>
                
                <div class="profile-stats mt-4">
                    <div class="stat-item">
                        <strong>{{ user_chat_rooms|length }}</strong>
                        <span>Conversas</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Conteúdo Principal -->
    <div class="col-md-9">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="bi bi-speedometer2 me-2"></i>Dashboard
                </h5>
            </div>
            <div class="card-body" style="max-height: 600px; overflow-y: auto;">
                <div class="row">
                    <!-- Estatísticas Rápidas -->
                    <div class="col-md-4 mb-4">
                        <div class="stat-card bg-primary text-white">
                            <div class="stat-icon">
                                <i class="bi bi-chat-left-text"></i>
                            </div>
                            <div class="stat-info">
                                <h3>{{ user_chat_rooms|length }}</h3>
                                <p>Conversas Ativas</p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-4 mb-4">
                        <div class="stat-card bg-success text-white">
                            <div class="stat-icon">
                                <i class="bi bi-people"></i>
                            </div>
                            <div class="stat-info">
                                <h3>{% widthratio user_chat_rooms|length 1 2 %}</h3>
                                <p>Conexões</p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-4 mb-4">
                        <div class="stat-card bg-info text-white">
                            <div class="stat-icon">
                                <i class="bi bi-check-circle"></i>
                            </div>
                            <div class="stat-info">
                                <h3>0</h3>
                                <p>Tarefas Hoje</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Conversas Recentes -->
                <div class="mt-4">
                    <h6>Conversas Recentes</h6>
                    {% if user_chat_rooms %}
                        <div class="list-group">
                            {% for room in user_chat_rooms|slice:":5" %}
                            <a href="#" class="list-group-item list-group-item-action"
                               onclick="window.chatManager.openChatDialog('{{ room.id }}', '{{ room.name|escapejs }}'); return false;">
                                <div class="d-flex w-100 justify-content-between">
                                    <h6 class="mb-1">{{ room.name }}</h6>
                                    <small class="text-muted">
                                        {% if room.room_type == 'DM' %}
                                            <i class="bi bi-person"></i>
                                        {% elif room.room_type == 'GROUP' %}
                                            <i class="bi bi-people"></i>
                                        {% else %}
                                            <i class="bi bi-check-square"></i>
                                        {% endif %}
                                    </small>
                                </div>
                                <p class="mb-1 text-muted">Clique para abrir conversa</p>
                            </a>
                            {% endfor %}
                        </div>
                    {% else %}
                        <div class="text-center py-4">
                            <i class="bi bi-chat-left" style="font-size: 3rem; color: #6c757d;"></i>
                            <p class="text-muted mt-2">Nenhuma conversa ativa</p>
                            <button class="btn btn-primary mt-2" data-bs-toggle="modal" data-bs-target="#novaConversaModal">
                                <i class="bi bi-plus-circle me-2"></i>Iniciar Conversa
                            </button>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<!-- JavaScript específico do perfil -->
<script src="{% static 'js/usuario/profile.js' %}"></script>
{% endblock %}